{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "build"
  },
  "functions": {
    "predeploy": [
      "npm --prefix functions run lint",
      "npm --prefix functions run build"
    ]
  }
}
